#include<iostream.h>
#include<conio.h>
using namespace std;

main()
{
      float num1, num2, num3, sum, ave;
      
      cout<<"Enter 3 numbers: ";
      cin>>num1>>num2>>num3;
      cout<<endl;
      cout<<endl;
      sum = num1 + num2 + num3;
      ave = sum / 3;
      cout<<"The sum is "<<sum<<" and the average is "<<ave<<"";
      
      getche();
}
      
